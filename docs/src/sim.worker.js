// Reserved for a future enhancement: run simulate() off the main thread for
// large N so the page stays responsive. Currently unused — the simulation
// runs synchronously in main.js, which is fast at the default sizes.
